import { add, noop } from '../src/'

test('adds 1 + 2 to equal 3', () => {
  expect(add(1, 2)).toBe(3)
})

it('should ', () => {
  expect(noop()).toBe(void 0)
})

it('should ', () => {
  expect(noop()).toBe(void 0)
})

describe('noop', () => {
  expect(add(1, 2)).toBe(3)
})
